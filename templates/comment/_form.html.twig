{{ form_start(form) }}
<div class="form-group row">
    <div class="col-sm-4 col-form-label text-left">
        {{ form_label(form.product) }}
    </div>

    <div class="col-sm-8">
        {{ form_widget(form.product) }}
    </div>
</div>
<div class="form-group row">
    <div class="col-sm-4 col-form-label text-left">
        {{ form_label(form.rate) }}
    </div>

    <div class="col-sm-8">
        {{ form_widget(form.rate) }}
    </div>
</div>
<div class="form-group row">
    <div class="col-sm-4 col-form-label text-left">
        {{ form_label(form.content) }}
    </div>

    <div class="col-sm-8">
        {{ form_widget(form.content) }}
    </div>
</div>

    <button class="btn ml-3 btn-outline-secondary text-success mt-5">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
