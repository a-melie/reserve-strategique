{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-secondary text-success mt-3">{{ button_label|default('Valider') }} <i class="far fa-paper-plane"></i></button>
{{ form_end(form) }}
