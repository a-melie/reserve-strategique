# reserve-strategique
checkpoint4


    * clone repository (git clone) 
    * run composer install
    * run yarn install
    * run yarn encore dev
    * run symfony server:start
        Then go to localhost url

